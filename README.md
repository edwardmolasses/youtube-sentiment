# Youtube Sentiment

## Installation

in root directory:

```npm install```

in client directory:

```npm install```

## Usage

in root directory:

```npm start```

in client directory:

```npm start```
